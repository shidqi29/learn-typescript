"use strict";
describe("Data Type", () => {
    it("should must declare", () => {
        const name = "Shidqi Amirul Haq";
        const balance = 100000;
        const isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
